<td>
    <%= attachment_image_tag book_comment.user , :profile_image, :fill, 60, 60, class: "pull-left profile-thumb", fallback: "no_image-icon.jpg" %>
    <%= book_comment.user.name %>
</td>
<td><%= book_comment.comment %></td>
<td>
    <% if book_comment.user == current_user %>
        <%= link_to "削除", book_book_comment_path(book_comment.book_id, book_comment.id), method: :delete, remote: true, class:"btn btn-sm btn-danger" %>
    <% end %>
</td>